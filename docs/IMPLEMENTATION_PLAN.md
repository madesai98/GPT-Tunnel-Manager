# GPT Tunnel Manager Implementation Plan

Status: **v2 planning complete / implementation not yet started**

The canonical implementation contract for the next major release is:

`docs/V2_IMPLEMENTATION_PLAN.md`

The previous v1 implementation contract remains available in repository history at the v1.0.32 planning baseline commit:

`08366ffbd299177870c10a3446ab9e4dcd35a18e`

V2 is intentionally a clean product/configuration break. Do not carry v1 migration, compatibility, per-server tunnel/plugin, lifecycle marker, or lifecycle skill requirements forward unless a later accepted ADR explicitly reintroduces them.

See `CONTEXT.md` for canonical v2 terminology and `docs/adr/0009-*` through the latest accepted ADR for architectural decisions.
